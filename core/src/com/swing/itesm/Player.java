package com.swing.itesm;

import com.badlogic.gdx.graphics.Texture;

public class Player extends  Objeto {
    public Player(Texture textura, float x, float y) {
        super(textura, x, y);
    }
}
